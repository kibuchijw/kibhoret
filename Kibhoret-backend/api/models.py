# from django.db import models
# from tastypie.resources import ModelResource
# from backend.models import GateEntry

# # Create your models here.
# class GateEntryResource(ModelResource):
#     class Meta:
#         queryset = GateEntry.objects.all() #returns a query that will be loaded at a given time in the future
#         resource_name = 'gate'
